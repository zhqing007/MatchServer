<Window x:Class="WpfMatchClient.SelectMatch"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="打开" Height="389" Width="373">
    <Grid>
        <Label Content="赛事列表："
               Height="28"
               HorizontalAlignment="Left"
               Name="label1"
               VerticalAlignment="Top" />
        <ListBox Height="274"
                 HorizontalAlignment="Left"
                 Margin="0,24,0,0"
                 Name="matchlistBox"
                 VerticalAlignment="Top"
                 Width="351" />
        <Button Content="打开"
                Height="23"
                HorizontalAlignment="Left"
                Margin="58,312,0,0"
                Name="open"
                VerticalAlignment="Top"
                Width="75"
                Click="open_Click" />
        <Button Content="取消"
                Height="23"
                HorizontalAlignment="Left"
                Margin="214,312,0,0"
                Name="cancle"
                VerticalAlignment="Top"
                Width="75"
                Click="cancle_Click" />
    </Grid>
</Window>
